import React from 'react';
import {StyleSheet, View} from 'react-native';
import {TextComponent} from '../../atoms';

const LoginScreen = () => {
  return (
    <View>
      <TextComponent text={'THIS IS LOGIN SCREEN'} />
    </View>
  );
};

const styles = StyleSheet.create({});

export default LoginScreen;
